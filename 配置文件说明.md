# Backupper 配置文件说明

配置文件为 `config.json`，与 `backup_runner.py` 放在同一目录下。

首次运行时若文件不存在会自动生成默认配置，编辑后重新运行即可。

## 配置项

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `server_type` | 字符串 | `"bds"` | 服务器类型。可选：`"bds"` / `"levilamina"` / `"endstone"` |
| `server_path` | 字符串 | `""` | 服务器根目录的绝对路径，例如 `D:\\Servers\\BDS-test` |
| `backup_interval_minutes` | 数字 | `1440` | 备份间隔（分钟）。1440 = 每 24 小时 |
| `backup_keep_count` | 数字 | `7` | 保留最近 N 个备份，超出自动删除 |
| `backup_max_age_days` | 数字 | `14` | 超过 N 天的旧备份自动删除 |

## 服务器类型差异

| 类型 | 启动命令 | worlds 位置（相对于 server_path） |
|---|---|---|
| `bds` | `bedrock_server.exe` | `worlds/` |
| `levilamina` | `bedrock_server_mod.exe` | `worlds/` |
| `endstone` | `endstone` | `bedrock_server/worlds/` |

## 示例

```json
{
  "server_type": "bds",
  "server_path": "D:\\Servers\\MyServer",
  "backup_interval_minutes": 1440,
  "backup_keep_count": 7,
  "backup_max_age_days": 14
}
```

## 备份流程

1. 发送 `save hold` 暂停服务器写入
2. 每秒发送 `save query` 直到服务器返回 `Data saved`
3. 复制 `worlds` 文件夹到 `backups/` 目录
4. 发送 `save resume` 恢复服务器写入
5. 压缩为 `worlds_backup_YYYYMMDD_HHmmss.zip`
6. 清理过期备份

备份文件存储在 `{server_path}/backups/` 目录下。
